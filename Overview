#### First We need server to so we , create aws account and use the aws elastic cloud compute (Ec2).
### Connect the server with remote control access (Mobaxterm) and we can run commands .####
### We install Jenkins for creating pipeline in Git hub action .

####Creating pipeline in git hub action :-

name: Build and Test Java Spring Boot

on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        distribution: 'adopt'
        java-version: '11'

    - name: Build with Maven
      run: |
        ./mvnw clean install -B

    - name: Run Tests
      run: |
        ./mvnw test






